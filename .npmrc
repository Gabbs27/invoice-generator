# Resolver siempre las dependencias peer, diga lo que diga la configuración del
# usuario. Con legacy-peer-deps=true npm no instala vite (peer obligatorio de
# Vitest 5) y escribe un package-lock.json que `npm ci` rechaza en cualquier
# máquina con la configuración por defecto.
legacy-peer-deps=false
